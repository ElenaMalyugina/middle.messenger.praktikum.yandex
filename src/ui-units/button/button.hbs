<button class="{{className}}" type="{{type}}" title="{{text}}" id="{{id}}" ref="{{ref}}" data-action="{{action}}">
    {{#if icon}}
        {{{ renderIcon icon }}}
    {{/if}}
    {{{ text }}}
</button>
