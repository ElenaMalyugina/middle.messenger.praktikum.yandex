<label class="{{className}}" for="{{name}}" title="{{title}}">
    {{#if icon}}
        {{{ renderIcon icon }}}
    {{/if}}
    <span class="popup__text">{{text}}</span>
    <input class="visually-hidden" type="file" name="{{name}}" id="{{name}}" form="{{currentForm}}">
</label>
