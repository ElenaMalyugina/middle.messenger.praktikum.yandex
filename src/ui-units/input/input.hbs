<input
    class="{{className}}"
    type="{{type}}"
    name="{{name}}"
    id="{{id}}"
    placeholder="{{placeholder}}"
    value="{{value}}"
    required="{{required}}"
    data-validators="{{validators}}"
    ref="{{ref}}"
    />
